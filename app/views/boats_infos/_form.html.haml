= form_for @boats_info do |f|
  - if @boats_info.errors.any?
    #error_explanation
      %h2= "#{pluralize(@boats_info.errors.count, "error")} prohibited this boats_info from being saved:"
      %ul
        - @boats_info.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
